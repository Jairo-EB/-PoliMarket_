package bodega;

public class GenerarEntrada {
    public void registrarEntrada(String productoId, int cantidad) {
        System.out.println("Entrada registrada para producto " + productoId + " con cantidad " + cantidad);
    }
}
