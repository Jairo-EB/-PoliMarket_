package bodega;

public class HacerSalida {
    public void registrarSalida(String productoId, int cantidad) {
        System.out.println("Salida registrada para producto " + productoId + " con cantidad " + cantidad);
    }
}
